export interface StandardFormData {
  code: string;
  name: string;
  description: string;
  order: string;
  published: boolean;
}

export interface EvidenceFormData {
  code: string;
  name: string;
  category: string;
  order: string;
  published: boolean;
}
